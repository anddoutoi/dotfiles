# commads
alias c='clear'

alias ll='ls -l'
alias lls='ls -a'
alias lsa='ls -a'
alias lla='ls -al'
alias lsh='ls -d .*'  # list hidden
alias llh='ls -dl .*' # list hidden

alias reload='. ~/.bash_profile'

# apps

# tools
